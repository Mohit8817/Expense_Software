# Copy to .env before building. Vite bakes this into the production bundle at build time.

# Local development
# VITE_BACKEND_API_URL=http://localhost:5001/api/

# Production (set your live API base URL, must end with /api/)
VITE_BACKEND_API_URL=https://klk.co.in/api/
